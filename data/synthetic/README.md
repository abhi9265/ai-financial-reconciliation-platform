# Synthetic seed data

The seed dataset is synthetic and deterministic. It is designed for development and evaluation, not for representing real customers or real financial activity.

It contains controlled cases for exact matches, vendor-name variations, invoice-date differences, amount mismatches, and missing invoices.

The ground-truth file is separate from the source records so reconciliation predictions can be evaluated against known outcomes.
